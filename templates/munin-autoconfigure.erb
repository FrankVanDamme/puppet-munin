#!/bin/sh
# File Managed by Puppet

<% if scope.lookupvar('munin::autoconfigure_clean') %>/bin/rm -f <% end %><%= scope.lookupvar('munin::conf_dir_active_plugins') %>* ; /usr/sbin/munin-node-configure --shell 2> /dev/null | /bin/sh 
/etc/init.d/<%= scope.lookupvar('munin::service') %> <%= scope.lookupvar('munin::restart_or_reload') %> >/dev/null 2>&1
